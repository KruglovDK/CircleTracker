[project]
name = "circletracker"
version = "0.1.0"
description = "Future production-ready expense tracker"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.128.0",
    "uvicorn>=0.40.0",
]
